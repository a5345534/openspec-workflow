# demo-project

This directory only demonstrates the shape of a project overlay.

Copy `.openspec-workflow.yaml` into a real project root, then run the workflow scripts from the repository root or from an installed copy of `openspec-workflow`.
